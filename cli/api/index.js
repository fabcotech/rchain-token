const { deployMaster } = require('./deployMaster');
const { deployBox } = require('./deployBox');
const { deploy } = require('./deploy');

module.exports = {
    deployMaster,
    deployBox,
    deploy
};